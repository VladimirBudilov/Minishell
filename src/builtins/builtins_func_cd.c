#include "../../includes/minishell.h"



void cd_func(t_token **token)
{
    if (!(*token)->next == NULL)
    {

    }
    else if (!(ft_strncmp((*token)->next->data), "-", 1))
    {

    }
    else
    {
        
    }
}